##########################################################################
# FieldCheck AI — backend dependencies
##########################################################################

# --- Web framework -------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Settings / validation -------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---------------------------------------------------------------
sqlalchemy==2.0.35
greenlet==3.1.1        # required by SQLAlchemy's async engine; not always pulled in transitively
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.2

# --- Async task queue (optional Celery path; BackgroundTasks used by default)
celery==5.4.0
redis==5.0.8

# --- Vision / AI provider SDKs ----------------------------------------------
openai==1.51.0
anthropic==0.34.2

# --- Imaging / file handling -------------------------------------------------
Pillow==10.4.0
python-magic==0.4.27
aiofiles==24.1.0

# --- HTTP client (demo scripts, retries) -------------------------------------
httpx==0.27.2
tenacity==9.0.0

# --- Templating / report generation ------------------------------------------
Jinja2==3.1.4

# --- Environment ----------------------------------------------------------
python-dotenv==1.0.1

# --- Testing ----------------------------------------------------------------
pytest==8.3.3
pytest-asyncio==0.24.0
